
{{#each cat}}
    <div class="card mt-2">
        <div class="card-body">
            <h4>Categoria: {{nome}}</h4>
            <a href="/categoria/{{_id}}"> <button class="btn btn-outline-dark mt-1">Visualizar Postagems dessa Categoria</button> </a>
            <br>
        </div>
    </div>
{{else}}
    <h4>Nenhuma Categoria registrada.</h4>
{{/each}}