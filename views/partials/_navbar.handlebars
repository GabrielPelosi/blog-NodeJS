<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <a class="navbar-brand" href="#">Lolosi's Blog</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <a class="nav-link" href="/">Home <span class="sr-only">(current)</span></a>
      </li>
      <li class="nav-item">
        <a class="nav-link" href="/categoria">Categorias</a>
      </li>
        {{#if user}}
            <li class="nav-item">
                <a class="nav-link" href="/usuario/logout">Logout</a>
            </li>
        {{else}}
            <li class="nav-item">
                <a class="nav-link" href="/usuario/login">Login</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="/usuario/registro">Registro</a>
            </li>
        {{/if}}
    </ul>
      {{#if user}}
      <form class="form-inline my-2 my-lg-0" action="/usuario/perfil">
          <label class="text text-white p-lg-2">{{user.nome}}</label>
          <button type="submit" class="btn btn-outline-light">Perfil</button>
      </form>
          {{/if}}


  </div>
</nav>